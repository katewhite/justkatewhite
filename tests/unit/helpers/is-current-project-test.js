import { isCurrentProject } from 'justkatewhite/helpers/is-current-project';
import { module, test } from 'qunit';

module('Unit | Helper | is current project');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = isCurrentProject([42]);
  assert.ok(result);
});
